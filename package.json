{
  "name": "sir-zain",
  "version": "1.0.0",
  "description": "Simple HTML with Tailwind CSS",
  "scripts": {
    "build-css": "tailwindcss -i ./input.css -o ./output.css --minify",
    "dev": "tailwindcss -i ./input.css -o ./output.css --watch",
    "build": "tailwindcss -i ./input.css -o ./output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}

